apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: whoami-helm
  namespace: argocd
spec:
  project: default

  sources:
    - repoURL: oci://ghcr.io/carlosedp/helm-charts/whoami
      chart: whoami
      targetRevision: 0.1.2
      helm:
        valueFiles:
          - $values/apps/whoami-helm/values.yaml

    - repoURL: https://github.com/iessenov/gitops.git
      targetRevision: HEAD
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: whoami-helm

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true