replicaCount: 1

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: ingress-nginx-apps
  annotations: {}
  hosts:
    - host: whoami-helm.essenov.com
      paths:
        - path: /
          pathType: Prefix
  tls: []